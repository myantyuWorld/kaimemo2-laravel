pre-commit:
  parallel: true
  commands:
    format:
      root: "src/"
      run: "npm run format"
      stage_fixed: true
    lint:
      root: "src/"
      run: "npm run lint"
      stage_fixed: true
    pint:
      root: "src/"
      run: "./vendor/bin/pint"
      stage_fixed: true
    test:
      root: "src/"
      run: "composer run test"